# research-loom —— 文献→设计 工作方法

> 本篇是**本项目自身的研究→设计工作方法**，非 autoharness 产品架构（产品架构见 [workflow.md](workflow.md)）。
> 操作指南由 `research-loom` skill 承载；内容产物落在 `docs/research-loom/`，其字段契约见该目录索引。

## 原则

**把"读文献到出设计"做成一条原子稳定、视图可弃、全程可溯的流水线，使想法随阅读演化而不必重构。**
病根在于把思考存成一棵不断生长的树：树逼人在想清前先定层级，理解一变层级即错位，于是重构、且旧结论被覆盖而遗忘。
解法是拆开**原子**（小而自洽、有稳定标识、几乎不动）与**视图**（矩阵、脊柱图、思维导图等廉价可弃的投影）：原子稳定则"想法变了"只意味着加原子、重画视图，而非重构。

## 流水线

```
论文 ─► ①捕获 ─► ②组织 ─► ③蒸馏 ─► ④装配 ⇄ ⑤精炼 ─► 定稿
        1-sources  2-directions  3-ideas   4-design   5-decisions
                                              │
                              provenance：设计 ─► idea ─► 论文
```

- **① 捕获** —— 每篇沉淀为一张**文献笔记**：核心论点、做法、优缺点。一源一卡。
- **② 组织** —— 把文献笔记聚成**方向**（每个方向一张 MOC），同方向内出概念矩阵横向对比，并附"我的评判"。按概念组织，不按作者。
- **③ 蒸馏** —— 把认可的思路与自己的新念头提为**永久笔记**：用自己的话、原子化、引用所凭的文献笔记。永久笔记不是论文摘要，是你的主张。
- **④ 装配** —— 把永久笔记织成**设计草稿**（脊柱图），每个设计元素连起 provenance 链。
- **⑤ 精炼** —— 对设计上有争议的点跑决策模型（驱动力→选项→权衡→取舍），坍缩成一条决策记录并改图。

## 不变量

1. **三种卡分家。** 文献笔记（论文说）、永久笔记（我说）、设计（组装）是三类不同的原子；文献↔永久分家，才使念头能自由重组进设计而不必重构。
2. **provenance 可溯。** 任何设计元素经永久笔记可一路追回支撑它的文献，"链接到各论文"由此自动成立。
3. **④⇄⑤ 是循环，非单向。** 设计是被决策反复改写的脊柱（状态），决策是回灌设计的精炼引擎；精炼以已有草稿为前提，故装配在前。
4. **决策只增不删（append-only）。** 旧决策被取代而非抹除，留下"为何如此、后又为何改"的轨迹，对抗遗忘。
5. **视图由原子投影，不单独维护。** 概念矩阵、脊柱图、思维导图皆从原子的结构化元数据生成，可弃可重建；唯原子长期供养。
6. **载体中立、可升级。** 原子为纯文本加结构化元数据与卡间链接，既可在纯仓库中检索，亦可被笔记工具渲染为自动视图，二者读同一份原子。

## 与产品设计树的过渡

`4-design` 是将来唯一的设计家；现有 `docs/design/`（含本篇）在过渡期冻结为 legacy，新设计落 `4-design`，过渡期以 `4-design` 为准，日后整体迁移。此举避免两个真相源。
